"use server";
import { getDataList } from "@/app/lib/actions";
export default async function MovieSliders() {
  //   await new Promise((resolve) => setTimeout(resolve, 5000));
  //   const list = await getDataList("popular", "movie");
  return <></>;
}
